# 引継ぎメモ（handoff）

セッションをまたぐ**揮発的な引継ぎメモ**。**このファイルは毎回上書き**（最新1件だけを保持する）。

- 製品の定義（誰が使うか・差別化点・書類の仕様・作り直しの設計）→ `docs/design.md`
- **実装の受け入れ条件・テスト方針・デバッグ方針・PRの区切り** → `docs/plan-rebuild.md`
- 過去の失敗の蓄積 → `docs/failures.md`（append-only・消さない）
- 開発の進め方・規約 → `AGENTS.md`

**恒久的な決定をこのファイルに書かない。** 上書きされて消えるうえ、`docs/design.md` /
`docs/plan-rebuild.md` と食い違ったときに、次のセッションが古い仕様を実装してしまう。

## ①今回実施

- **PR-A（スキーマ＋DB層）を実装し、マージ済み。**
  `main` の `d92172b`（PR #31）。明細行の安定ID・法定項目スロット（9キー）・
  施工条件・範囲リスト（12区分）・会社設定・下請台帳・依頼グループの
  マイグレーションとDB層、対応する単体テストを追加した。CodeRabbitの指摘13件
  （IDOR対策・明細ID検証・マイグレーションの順序保証・backfillの網羅性・
  design.md/plan-rebuild.mdの記述矛盾）を修正済み。CIは全緑（`ci-green`）。
- PR-Aマージ後、`AGENTS.md`の規則どおりブランチを `origin/main` から作り直した
  （`git checkout -B claude/new-app-init-hufsv1 origin/main`）。
- **PR-Bに着手。** `docs/plan-rebuild.md` B-1（書類UIスパイク）を実装した：
  `apps/web/app/projects/spike-document/page.tsx`。保存もDBも使わない使い捨てで、
  対話可能な箇所は「キッチン工事」の1行だけ。A4書類をResizeObserverで画面幅に
  合わせて縮小表示し、行ブロックを押すと下からシートが出て写真撮影・数量入力を
  行う。写真枠（200px四方）は書類上で直接タップしてカメラを起動できる。
  typecheck / lint 通過済み。

## ②今回トラブル

特になし。PR-Aは（CodeRabbitの指摘対応を除き）想定どおり進んだ。

## ③次回やる事

**このスパイクをdraft PRとしてpushし、Vercelプレビューを実機のスマホで確認する。**
`docs/plan-rebuild.md` B-1 の受け入れ条件 S1〜S6 は人が触って判定する
（機械判定にしない）：

- S1: 書類が「書類の形」に見える
- S2: 行ブロックを親指で押せる。押し間違えない
- S3: シートが出たら本文16px・タップ48pxで、拡大せずに数量を打てる
- S4: シートを閉じると書類の該当セルに値が入ったのが見える
- S5: 写真枠のカメラマークを直接押せる
- S6: 200%まで拡大しても情報・機能が失われない

**S1〜S5のどれかが崩れたら、本実装（`apps/web/lib/doc/` へのテンプレート抽出）に
進まず報告する。** 見せ方を変えるかどうかは設計判断であり実装者が決めることではない
（`docs/plan-rebuild.md` 参照）。

実機確認が通ったら、PR-B の続き（印刷CSS→テンプレート抽出）に進む。

## ④まだ埋まっていない前提

- 依頼提示後に写真を追加したときの扱い（法的評価は未確認）
- 期限を過ぎた回答の扱い
- 諸経費率の実際の数字、掛率、見積有効期限の日数（デモ用に決め打ちでよい）
- 本番で実在の顧客情報を扱うときの社外ホスティング可否
- **法定③（設計図書＝写真＋数量）の充足判定条件**（未決。PR-Cの送信ゲート実装前に
  決める。CodeRabbitのPR-Aレビューでも指摘され、design.md 600行目に明記済み）
