# 引継ぎメモ（handoff）

セッションをまたぐ**揮発的な引継ぎメモ**。**このファイルは毎回上書き**（最新1件だけを保持する）。

- 製品の定義（誰が使うか・差別化点・書類の仕様・作り直しの設計）→ `docs/design.md`
- **実装の受け入れ条件・テスト方針・デバッグ方針・PRの区切り** → `docs/plan-rebuild.md`
- 過去の失敗の蓄積 → `docs/failures.md`（append-only・消さない）
- 開発の進め方・規約 → `AGENTS.md`

**恒久的な決定をこのファイルに書かない。** 上書きされて消えるうえ、`docs/design.md` /
`docs/plan-rebuild.md` と食い違ったときに、次のセッションが古い仕様を実装してしまう。

## ①今回実施

**デモを3タップにした（利用者からの指摘。最優先の変更）。**

本番URLは商談用のデモを兼ねているのに、価値（写真→複数社へ一斉依頼→横並び比較）まで
**46回の入力**を要求する状態で公開していた。売りそのものは3画面しかない。
経緯と数え方は `docs/failures.md` 2026-08-06 の6件目、決定は `docs/design.md` 6-1。

- `lib/auth/demoOwner.ts` … `demo:<uuid>` の使い捨て識別子。**通常のセッションCookieに
  載せる**ので、proxy・getCurrentUser・owner_id の絞り込みを1つも変えずに
  無ログインのデモが成立する。訪問ごとに識別子が違うので、商談が2件同時でも混ざらない。
- `lib/db/demoSeed.ts` … seed の中身をここへ移した。アプリの「デモを触ってみる」と
  CLI（`scripts/seed-demo.ts`）が同じ関数を通る。`apps/web/scripts/seed-demo.mjs` は削除。
- 経路：トップ「デモを触ってみる」→ `/demo/<id>/photo`（撮る／写真なしで進む）
  → 比較表（3社が返信済み・見積書ボタン・法定項目21件の案内）→ PDF。**3タップ**。
- **機能は1つも削っていない。** 会社設定・下請台帳・法定項目21件はデモ利用者にも
  通常どおり開ける。入力させずに入った状態にしただけ。
- 検査：`e2e/demo-three-taps.spec.ts`（**seed を使わない**。タップは `TAPS` 配列そのもの
  なので、ボタンが1つ増えたら落ちる）／`tests/demoSeed.test.ts`（投入直後に3社の回答が
  並ぶ・送信ゲートを通る・2回入れても増えない・他人のデモは見えない）／
  `tests/demoOwner.test.ts`／`scripts/smoke.sh`（未ログインのトップにデモの入口・
  デモ未開始で `/demo/<uuid>/photo` が404）。
- `tsx` を devDependency に追加（CLI から TS の共通モジュールを呼ぶため）。

**ローカル検証は全部通っている**：typecheck / lint / test 258件 / `pnpm -r build` /
`scripts/smoke.sh` / `scripts/e2e.sh` 10件。

**スキーマは変えていない。** `owner_id` は FK の無い text 列なので、デモの識別子を
入れるのに migration が要らなかった。本番DBへの追加適用は不要（下の表の下段）。

そのほか（同じPRに入っている）：

- `docs/manual.md` … 操作説明書。冒頭に「まず触ってみる（3回押すだけ）」を置き、
  それ以降を「実際に使いはじめる方向け」と位置づけ直した。
- 会社設定（#36）・トップ画面の導線（#38）・handoff の整理（#39 #40）は main にマージ済み。

これまでのPRはすべて main にマージ済み（各PRのマージコミット）：
PR-A（#31 `d92172b`）／PR-B・PR-C（#32 `e8321cd`）／レビュー修正（#33 `4b80fde`）／
入力画面の追加（#34 `9e98829`）／レビュー修正（#35 `249bd7c`）／
会社設定（#36 `b1dc868`）／レビュー修正（#37 `6e84a26`）／
トップ画面の導線（#38 `b858adf`）／handoff更新（#39 `ea80572`）／
handoffの本番DB記述の整理（#40）。

### 本番DBの状態（次に触る人が最初に見るところ）

**2つの話を混ぜない。** 片方は「これまでに作ったテーブルが本番に入っているか」、
もう片方は「今回の変更で追加の適用作業が要るか」で、答えが別々にある。

| | 状態 |
|---|---|
| `supabase/migrations/` にある既存のマイグレーション | **本番へ適用済み**（下記の prod-smoke が証拠） |
| 直近の変更（#36〜デモ対応）で増えたマイグレーション | **無い**。したがって**追加の適用作業は不要** |

適用済みの証拠は prod-smoke の結果に置く。コミットSHAはソースが変わったことしか
示さないので、DBの状態は別の外部事実で示す：

- 適用前（赤）: [run 31066836650](https://github.com/rahiseko-alt/renovation-estimate-app/actions/runs/31066836650)
  … 新テーブルが無く、DBに到達する経路 `/q/<uuid>` が 500
- 適用後（緑）: [run 31068747260](https://github.com/rahiseko-alt/renovation-estimate-app/actions/runs/31068747260)
  … 同じ経路が 200

**次にスキーマを変えたときは、この表の下段が「有り」に変わる。** そのときは
`AGENTS.md`「本番DBへのマイグレーション適用」のとおり、利用者が自分の手元で
`supabase db push` を流す（エージェントは接続情報を受け取らない）。

## ②今回トラブル

`docs/failures.md` 2026-08-06 の3件目を見る。
データ層・送信ゲート・比較表・回答画面は全部動いていたのに、**入力する画面が無いせいで
本番では依頼を1件も出せなかった**。seed を前提にした検査が、その欠落を隠していた。

## ③次回やる事

1. **本番にマージしたら、実機のスマホでデモの3タップを確かめてもらう。**
   トップ →「デモを触ってみる」→「写真を撮る」→「見積書PDFを出力」。
   E2E は「写真なしで進む」側しか通していない（実ブラウザでカメラを扱えないため）。
   **実機のカメラ経路だけは機械で見られていない。**
2. **S1〜S6 の実機確認**（持ち越し。S2・S3 が未確認）。デモの3タップで作られる案件を
   そのまま使えば、会社設定を入れる手間なく書類画面まで行ける。
   - S2: 行ブロックを親指で押せる。押し間違えない ← **未確認**
   - S3: シートが出たら本文16px・タップ48pxで、拡大せずに数量を打てる ← **未確認**

   **S1〜S5 のどれかが崩れていたら、見せ方の作り直しになる。** 実装を進めずに報告して決める。
3. `/projects/spike-document` が固定のサンプルデータのままで、案件と繋がっていない。
   **S1〜S5 の結果次第で作り直しになる箇所**なので、確認をもらってから着手する。
4. デモ案件の後始末（未決）。無ログインの訪問ごとに案件が1件増える。
   商談で使う頻度なら問題にならないが、公開して放置すると溜まる。
   古いデモ案件を消す仕組みは入れていない。
5. 社印の取り扱い（会社設定に画像を持たせるか、印影なしで運用するか）。

## ④まだ埋まっていない前提

- 依頼提示後に写真を追加したときの扱い（法的評価は未確認）
- 期限を過ぎた回答の扱い
- 諸経費率の実際の数字、掛率、見積有効期限の日数（デモ用に決め打ちでよい）
- 本番で実在の顧客情報を扱うときの社外ホスティング可否
- **法定③（設計図書＝写真＋数量）の充足判定条件**（未決）。
  **送信ゲートは③を検査していない**（`lib/db/submissionGate.ts` が見るのは
  法定④⑤⑥⑧の9スロットと⑦の12区分だけ）。写真が0枚・数量が全部0でも送信できる。
  実運用に出す前に、充足条件を一次情報から決めて判定とテストを足すこと。
  デモの範囲では、写真と数量を入れて見せるので露見しない。
- 採用したときに単価をどの時点で見積へ書き戻すか（未決）
