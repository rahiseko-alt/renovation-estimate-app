# 引継ぎメモ（handoff）

セッションをまたぐ**揮発的な引継ぎメモ**。**このファイルは毎回上書き**（最新1件だけを保持する）。

- 製品の定義（誰が使うか・差別化点・書類の仕様・作り直しの設計）→ `docs/design.md`
- 過去の失敗の蓄積 → `docs/failures.md`（append-only・消さない）
- 開発の進め方・規約 → `AGENTS.md`

**恒久的な決定をこのファイルに書かない。** 上書きされて消えるうえ、`docs/design.md` と
食い違ったときに、次のセッションが古い仕様を実装してしまう。

## ①今回実施

- **本番が完全に壊れていたのを復旧した。** 原因はVercelの本番環境変数が未設定だったことと、
  本番Supabaseにテーブルが未作成だったこと。監視がすり抜けていたので検査を追加し、
  PR #29 でマージ済み（squash commit `19b885e`、本番への `workflow_dispatch` run
  31048799160 が success）。
- **設計の作り直しが決まり、確定内容を `docs/design.md` に書いた**（PR #30、
  squash commit `1ee5515`）。同時に `AGENTS.md` へ「業務ドメインを決めるときの作法」を追加。
- `docs/failures.md` に本日付で7件追記（既存行の削除なし）。

## ②今回トラブル

`docs/failures.md` 2026-08-05 を見る。

## ③次回やる事

**`docs/design.md` を通しで読んでから、7章の作り直しを実装する。**
実装の順番と対象は design.md 7章「現行実装からの変更点」の表を正とする。
このメモに設計内容を書き写さない。

- PR #30 のマージ直前に届いた CodeRabbit の指摘3件は、マージに間に合わず未適用。
  次のPRの最初のコミットで対応する（法定8項目を項目ごとに保持する／「未定」を
  未入力の穴埋めに使わない／見積期間の元になる予定価格帯の扱い）。

## ④まだ埋まっていない前提

- 諸経費率の実際の数字、掛率、見積有効期限の日数（デモ用に決め打ちでよい）
- 本番で実在の顧客情報を扱うときの社外ホスティング可否
