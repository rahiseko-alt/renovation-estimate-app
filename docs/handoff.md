# 引継ぎメモ（handoff）

セッションをまたぐ**揮発的な引継ぎメモ**。**このファイルは毎回上書き**（最新1件だけを保持する）。

- 製品の定義（誰が使うか・差別化点・書類の仕様・調査で確定した事実）→ `docs/design.md`
- 過去の失敗の蓄積 → `docs/failures.md`（append-only・消さない）
- 開発の進め方・規約 → `AGENTS.md`

**恒久的な決定をこのファイルに書かない。** 上書きされて消える。

## ①今回実施

**本番障害の復旧と、製品方向性の見直し。**

- **本番が完全に壊れていたのを直した。** 原因は2つ（Vercelの本番環境変数が未設定／
  本番Supabaseにテーブルが未作成）。利用者に画面を案内して復旧済み。
  監視がすり抜けていた（`prod-smoke.yml` がDBに触らないページしか見ていなかった）ので、
  DB到達検査とCSP検査を追加し PR #29 でマージ済み（squash commit `19b885e`、
  本番に対する `workflow_dispatch` の run 31048799160 が success）。
- **今の実装は前提から間違っていたことが判明し、設計の作り直しが決まった。**
  確定した内容はすべて `docs/design.md` に書いた（差別化点・書類①②・業務フロー・
  ②の様式・参照した実物のリンク）。
- `docs/failures.md` に本日付で**6件**追記（既存行の削除なし）。
- `AGENTS.md` に「業務ドメインを決めるときの作法」節を追加（実物・一次情報で決める／
  経路の異なる複数で一致を確認する／競合を調べてから差別化を言う／リポジトリ内の
  記録を先に読む／実務者レビューを挟む）。記録3ファイルの使い分けも明記した。

## ②今回トラブル

`docs/failures.md` 2026-08-05 の6件を見る。今回の議論で判明したエージェント側の失敗は
4件（実物を見ずに業務ドメインを設計した／競合調査せず差別化を断定した／利用者の指示を
記録に落とせず同じ指示を2回させた／リポジトリ内にあった一次情報をWebで探し回った）。

## ③次回やる事

**設計の作り直しに着手する。** 着手前に `docs/design.md` を通しで読む。
未決の設計判断は design.md 7章にまとめてある（①の法定条件欄をどこまで自動で埋めるか、
写真を書類に埋め込むか別紙か、現行の見積エディタをどこまで残すか、
1明細＝1依頼＝1社 を複数社同時依頼にどう作り直すか）。

**任意・優先度低**：会社情報（請負者名・代表者・住所・印）の設定画面。
①②とも請負者情報が必須（実物5件すべてにある）。

## ④まだ埋まっていない前提

- 諸経費率の実際の数字、見積有効期限の日数（デモ用に決め打ちでよい）
- 本番で実在の顧客情報を扱うときの社外ホスティング可否

（「既存の見積書の現物」は解決済み。営業デモ用途のため現物は存在せず、Webから集めた
サンプルを参照して自前で作る方針で確定。→ `docs/design.md` 6章）
