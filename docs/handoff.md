# 引継ぎメモ（handoff）

セッションをまたぐ**揮発的な引継ぎメモ**。**このファイルは毎回上書き**（最新1件だけを保持する）。

- 製品の定義（誰が使うか・差別化点・書類の仕様・作り直しの設計）→ `docs/design.md`
- **実装の受け入れ条件・テスト方針・デバッグ方針・PRの区切り** → `docs/plan-rebuild.md`
- 過去の失敗の蓄積 → `docs/failures.md`（append-only・消さない）
- 開発の進め方・規約 → `AGENTS.md`

**恒久的な決定をこのファイルに書かない。** 上書きされて消えるうえ、`docs/design.md` /
`docs/plan-rebuild.md` と食い違ったときに、次のセッションが古い仕様を実装してしまう。

## ①今回実施

- **設計を独立検証3名（流用可能なもの／見落とし／その他）にかけ、`docs/design.md` を
  大幅改訂した。** 数値的に成立しない判断（書類の直接入力、画面とPDFの統一描画、
  法定8項目という数え方）、法令・実装との衝突（回答画面の作り、施主名の漏洩経路、
  取り込みが追加になっている）を訂正済み。
- **写真の置き場所・枚数・施工条件の提示方法を一次情報で確定させた。** 写真は別紙、
  1箇所1枚、施工条件は「施工条件・範囲リスト」への○×。
- **`docs/plan-rebuild.md` を新規作成。** 実装をSonnetに渡す前提で、受け入れ条件・
  テスト方針・デバッグ方針・PRの区切り（3本）を決めた。**次に着手するのはこれ。**

## ②今回トラブル

`docs/failures.md` 2026-08-05 を見る。設計の初稿に、実機を見ずに決めた数値的に
成立しない判断が複数あった（独立検証で発覚。詳細は追記予定）。

## ③次回やる事

**`docs/plan-rebuild.md` の PR-A から着手する。** 着手前に `docs/design.md` を
通しで読む。このメモに設計内容・受け入れ条件を書き写さない（plan-rebuild.md が正）。

- PR-A：スキーマ＋DB層＋単体テスト
- PR-B：書類UIスパイク→実機確認→印刷→テンプレート抽出（**S1〜S5 が崩れたら
  本実装に進まず報告する**）
- PR-C：依頼グループ／比較表と採用／回答画面／E2E

## ④まだ埋まっていない前提

- 依頼提示後に写真を追加したときの扱い（法的評価は未確認）
- 期限を過ぎた回答の扱い
- 諸経費率の実際の数字、掛率、見積有効期限の日数（デモ用に決め打ちでよい）
- 本番で実在の顧客情報を扱うときの社外ホスティング可否
