# 引継ぎメモ（handoff）

セッションをまたぐ**揮発的な引継ぎメモ**。**このファイルは毎回上書き**（最新1件だけを保持する）。

- 製品の定義（誰が使うか・差別化点・書類の仕様・作り直しの設計）→ `docs/design.md`
- **実装の受け入れ条件・テスト方針・デバッグ方針・PRの区切り** → `docs/plan-rebuild.md`
- 過去の失敗の蓄積 → `docs/failures.md`（append-only・消さない）
- 開発の進め方・規約 → `AGENTS.md`

**恒久的な決定をこのファイルに書かない。** 上書きされて消えるうえ、`docs/design.md` /
`docs/plan-rebuild.md` と食い違ったときに、次のセッションが古い仕様を実装してしまう。

## ①今回実施

PR-A（#31 `d92172b`）／PR-B・PR-C（#32 `e8321cd`）／レビュー修正（#33 `4b80fde`）は
すべて main にマージ済み。本番DBへのマイグレーション適用も完了している。

そのうえで**欠けていた入力画面2つを実装した**（`docs/failures.md` 2026-08-06 の3件目）：

- `/subcontractors` — 下請台帳。社名・メールの登録／削除。依頼の送り先はここから増やす。
- `/projects/[id]/legal` — 法定項目9スロット（記入する／未定／未入力）と
  施工条件12区分（○／×）の入力。1回の保存で21項目をまとめて書く。

あわせて：

- `lib/db/legalItemSlots.ts` / `lib/db/siteConditionChecks.ts` に一括保存
  （`setLegalItemSlots` / `setSiteConditionChecks`）を足した。1件ずつの関数は
  一括の経路を通るようにして、入口を1つに保っている。
- 導線を繋いだ：案件詳細 → 法定項目、案件一覧 → 下請台帳、送信画面の
  「足りない項目」「送り先が0件」から、それぞれ直しに行けるリンク。
- **seed に頼らないE2E** `apps/web/e2e/ledger-flow.spec.ts` を足した。
  ログイン後、画面の操作だけで「下請を登録 → 案件を作る → 法定項目を埋める →
  依頼を送る → 比較表」まで到達することを検査する。
- `scripts/smoke.sh` に `/subcontractors` の検査（未ログインは307・ログイン済みは200・
  登録フォームが出る）を足した。

**このPRはスキーマを変えていない。** 新しいマイグレーションは無いので、
本番DBへの適用作業は不要（長さの上限はアプリ側の境界で止めている）。

## ②今回トラブル

`docs/failures.md` 2026-08-06 の3件目を見る。
データ層・送信ゲート・比較表・回答画面は全部動いていたのに、**入力する画面が無いせいで
本番では依頼を1件も出せなかった**。seed を前提にした検査が、その欠落を隠していた。

## ③次回やる事

1. **本番URLで、実機のスマホから S1〜S6 を確認してもらう**（前回から持ち越し。
   S2・S3 は未確認）。**プレビューURLを渡さない・利用者に設定変更を頼まない**
   （`AGENTS.md`「実装の進め方」）。
   - S1: 書類が「書類の形」に見える
   - S2: 行ブロックを親指で押せる。押し間違えない ← **未確認**
   - S3: シートが出たら本文16px・タップ48pxで、拡大せずに数量を打てる ← **未確認**
   - S4: シートを閉じると書類の該当セルに値が入ったのが見える
   - S5: 写真枠のカメラマークを直接押せる
   - S6: 200%まで拡大しても情報・機能が失われない

   **S1〜S5 のどれかが崩れていたら、見せ方の作り直しになる。** 実装を進めずに報告して決める。

2. **会社設定の画面**（`/company`）。DB層 `lib/db/companyProfiles.ts` はあるが画面が無い。
   請負者名・代表者・住所と、法定④⑥⑧の定型文の初期値を持たせる画面。
   これが無いと、案件ごとに法定項目をゼロから打つことになる。

3. `/projects/spike-document` が固定のサンプルデータのままで、案件と繋がっていない。
   現場画面として本番の案件に繋ぐ作業が残っている。

## ④まだ埋まっていない前提

- 依頼提示後に写真を追加したときの扱い（法的評価は未確認）
- 期限を過ぎた回答の扱い
- 諸経費率の実際の数字、掛率、見積有効期限の日数（デモ用に決め打ちでよい）
- 本番で実在の顧客情報を扱うときの社外ホスティング可否
- **法定③（設計図書＝写真＋数量）の充足判定条件**（未決）。
  **送信ゲートは③を検査していない**（`lib/db/submissionGate.ts` が見るのは
  法定④⑤⑥⑧の9スロットと⑦の12区分だけ）。写真が0枚・数量が全部0でも送信できる。
  実運用に出す前に、充足条件を一次情報から決めて判定とテストを足すこと。
  デモの範囲では、写真と数量を入れて見せるので露見しない。
- 採用したときに単価をどの時点で見積へ書き戻すか（未決）
