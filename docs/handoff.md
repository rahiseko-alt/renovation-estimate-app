# 引継ぎメモ（handoff）

セッションをまたぐ**揮発的な引継ぎメモ**。**このファイルは毎回上書き**（最新1件だけを保持する）。

- 製品の定義（誰が使うか・差別化点・書類の仕様・作り直しの設計）→ `docs/design.md`
- **実装の受け入れ条件・テスト方針・デバッグ方針・PRの区切り** → `docs/plan-rebuild.md`
- 過去の失敗の蓄積 → `docs/failures.md`（append-only・消さない）
- 開発の進め方・規約 → `AGENTS.md`

**恒久的な決定をこのファイルに書かない。** 上書きされて消えるうえ、`docs/design.md` /
`docs/plan-rebuild.md` と食い違ったときに、次のセッションが古い仕様を実装してしまう。

## ①今回実施

**デモを3タップにした**（利用者からの指摘。PR #41 `ca8371b` でマージ済み）。

- 何を決めたか・なぜそう決めたかは **`docs/design.md` 6-1**（恒久的な決定はそちらが正）
- 失敗の経緯は **`docs/failures.md` 2026-08-06**
- 触り方は **`docs/manual.md`** 冒頭「まず触ってみる（3回押すだけ）」

**#41 のレビュー指摘6件＋nitpick 1件が、マージに間に合わず未処理のまま入った。**
このセッションの続きで直している（下記）。うち1件は実バグ：

- デモの署名付きセッションが**30日有効のまま発行されていた**。Cookie の maxAge だけを
  6時間にしていて、署名の中の失効時刻は既定値を使っていた。
  `createSessionValue` に有効期間を渡せるようにして直した。
- 無ログインの入口に歯止めが無かった → 連打時の作り直しをやめ、
  デモ開始のたびに期限切れの `demo:` 所有者を掃除するようにした
  （どこまでを持つかの線引きは `docs/design.md` 6-1）。
- `docs/handoff.md` に恒久的な決定を書いていた（この節を短くした）／
  ローカル検証を完了証拠のように書いていた／README の同期対象に `demoText.ts` が無かった／
  2つのデモが互いを開けないことのE2Eが無かった。

**手元での確認（これは完了証拠ではない。外部事実は下の CI / 本番URLで示す）**：
typecheck / lint / test 264件 / `pnpm -r build` / `scripts/smoke.sh` / `scripts/e2e.sh`。

外部事実：
- マージ済みPR … #41 `ca8371b`（デモ本体）
- 本番URL … https://renovation-estimate-app-web.vercel.app
- 続きのPRの CI 結果は、そのPRの run URL を見る

これまでのPRはすべて main にマージ済み（PR番号とマージコミット）：

- 土台と一連の流れ … `#31` `d92172b` / `#32` `e8321cd` / `#33` `4b80fde`
- 入力画面 … `#34` `9e98829` / `#35` `249bd7c`
- 会社設定 … `#36` `b1dc868` / `#37` `6e84a26`
- トップ画面の導線 … `#38` `b858adf`
- 記録の整理 … `#39` `ea80572` / `#40` `70fa441`
- デモの3タップ … `#41` `ca8371b` / `#42` `f1c220c`

### 本番DBの状態（次に触る人が最初に見るところ）

| | 状態 |
|---|---|
| `supabase/migrations/` にある既存のマイグレーション | **本番へ適用済み** |
| 直近の変更（#36〜#41 と続きのPR）で増えたマイグレーション | **無い**。**追加の適用作業は不要** |

適用済みの証拠（コミットSHAはソースが変わったことしか示さないので、DBの状態は別に示す）：

- 適用前（赤）: [run 31066836650](https://github.com/rahiseko-alt/renovation-estimate-app/actions/runs/31066836650) … `/q/<uuid>` が 500
- 適用後（緑）: [run 31068747260](https://github.com/rahiseko-alt/renovation-estimate-app/actions/runs/31068747260) … 同じ経路が 200

次にスキーマを変えたときの手順は `AGENTS.md`「本番DBへのマイグレーション適用」。

## ②今回トラブル

`docs/failures.md` 2026-08-06 の3件目を見る。
データ層・送信ゲート・比較表・回答画面は全部動いていたのに、**入力する画面が無いせいで
本番では依頼を1件も出せなかった**。seed を前提にした検査が、その欠落を隠していた。

## ③次回やる事

1. **実機のスマホでデモの3タップを踏んでもらう。** とくに**カメラ経路**。
   E2E は「写真なしで進む」側しか通せない（実ブラウザでカメラを扱えない）ので、
   **そこだけ機械で見られていない。**
2. **S1〜S6 の実機確認**（持ち越し。S2・S3 が未確認）。デモの3タップで作られる案件を
   そのまま使えば、設定を入れずに書類画面まで行ける。
   - S2: 行ブロックを親指で押せる。押し間違えない ← **未確認**
   - S3: シートが出たら本文16px・タップ48pxで、拡大せずに数量を打てる ← **未確認**

   **S1〜S5 のどれかが崩れていたら、見せ方の作り直しになる。** 実装を進めずに報告して決める。
   **S6（200%拡大で情報・機能が失われない）は、崩れていても直すだけで作り直しにはならない**
   ので、この判定の外に置いている（レイアウトの折り返しの問題で、見せ方の前提は変わらない）。
   もちろん崩れていれば直す。
3. `/projects/spike-document` が固定のサンプルデータのままで、案件と繋がっていない。
   **S1〜S5 の結果次第で作り直しになる箇所**なので、確認をもらってから着手する。
4. デモの入口に前段（Vercel の WAF 等）で流量制限を掛けるかの判断（未決。
   判断の材料は `docs/design.md` 6-1）。
5. 期限切れのデモを確実に消すための定時実行を入れるかの判断（未決。同上）。
6. 社印の取り扱い（会社設定に画像を持たせるか、印影なしで運用するか）。

## ④まだ埋まっていない前提

- 依頼提示後に写真を追加したときの扱い（法的評価は未確認）
- 期限を過ぎた回答の扱い
- 諸経費率の実際の数字、掛率、見積有効期限の日数（デモ用に決め打ちでよい）
- 本番で実在の顧客情報を扱うときの社外ホスティング可否
- **法定③（設計図書＝写真＋数量）の充足判定条件**（未決）。
  **送信ゲートは③を検査していない**（`lib/db/submissionGate.ts` が見るのは
  法定④⑤⑥⑧の9スロットと⑦の12区分だけ）。写真が0枚・数量が全部0でも送信できる。
  実運用に出す前に、充足条件を一次情報から決めて判定とテストを足すこと。
  デモの範囲では、写真と数量を入れて見せるので露見しない。
- 採用したときに単価をどの時点で見積へ書き戻すか（未決）
