# 引継ぎメモ（handoff）

セッションをまたぐ**揮発的な引継ぎメモ**。**このファイルは毎回上書き**（最新1件だけを保持する）。

- 製品の定義（誰が使うか・差別化点・書類の仕様・作り直しの設計）→ `docs/design.md`
- **実装の受け入れ条件・テスト方針・デバッグ方針・PRの区切り** → `docs/plan-rebuild.md`
- 過去の失敗の蓄積 → `docs/failures.md`（append-only・消さない）
- 開発の進め方・規約 → `AGENTS.md`

**恒久的な決定をこのファイルに書かない。** 上書きされて消えるうえ、`docs/design.md` /
`docs/plan-rebuild.md` と食い違ったときに、次のセッションが古い仕様を実装してしまう。

## ①今回実施

**デモを商談で見せられる状態にした。** デモが何のためにあるかは
**`docs/design.md` 6-1 が正**。ここには書かない。

利用者が実際に触って出てきた不具合を、報告のたびに直した。**全部、本番で確認済み。**

| | 開始時 | 現在 |
|---|---|---|
| 3タップ目の見積書 | **全行0円** | **906,448円** |
| 1タップ目 | 11.05 / 8.21 / 7.33 秒 | **0.75〜2.19秒** |
| 比較表 | 3.92秒 | **1.11秒** |
| デモの入口 | 押しても無反応（デプロイのたび再発） | URL方式で構造的に解消 |
| トップ画面 | ログイン状態で出し分け → デモに戻れない | 常に同じ。デモのボタンと黒丸だけ |
| 採用ボタン | 3社バラバラの文字 | 「採用」だけ。青と灰色 |
| デモの社名 | サンプル内装工業 等 | 業者A / 業者B / 業者C |
| 明細の「式」 | 4行中3行 | 1行（解体・廃棄物処理費） |

主な置き場所：

| 何 | どこ |
|---|---|
| 採用した下請の単価を見積に重ねる | `apps/web/lib/db/pricedEstimate.ts` |
| デモの入口（Server Action ではない） | `apps/web/app/demo/start/route.ts` |
| デモの中身の版（fixture から算出） | `apps/web/lib/demoVersion.ts` |
| デモに入れるデータ | `apps/web/lib/demoFixture.ts` |
| 本番を日次で踏む | `.github/workflows/prod-demo-check.yml` |

**マージ済み**：`#44` `7457177` / `#45` `6559f8c` / `#46` `4130402` /
`#47` `5249542` / `#48` `674e686` / `#49` `e80aef4` / `#50` `bda1d05`

### 本番の実測（外部事実。`bda1d05` 反映後）

```
PROD DEMO CHECK OK: 3タップの経路は本番で通り、待ち時間も上限内
デモ開始 2.188 / 0.747 / 1.051 秒（上限 3.5s）
比較表   1.113 秒（上限 3.0s）
合計     906,448円
```

**この計測点は日本ではない**ので、上の数字には計測元から東京までの通信時間が乗っている。
日本から使う商談では、これよりさらに速い。

### 本番DBの状態

| | 状態 |
|---|---|
| `supabase/migrations/` の既存マイグレーション | **本番へ適用済み** |
| 今回増えたマイグレーション | **無い**。**追加の適用作業は不要** |

次にスキーマを変えたときの手順は `AGENTS.md`「本番DBへのマイグレーション適用」。

## ②今回トラブル

`docs/failures.md` の 2026-08-06 後半〜2026-08-07 を見る。同じ形の失敗が繰り返し出た：

- **「動く」と「商談で見せられる」は別**。0円の書類、無反応のボタン、消えた入口は、
  どれも自動検査が緑のまま起きた。
- **検査が原理的に再現できない状態を作っていた**（毎回ページを取り直してから押していたので、
  「古いページから押す」が再現しなかった）。
- **遅いときに1回ぶんのコストを測らずに、回数だけ3回削った。** 原因は距離だった。
- **squash マージ後にブランチを作り直さなかった（4回目）。**

## ③次回やる事

1. **実機のスマホでデモの3タップを踏んでもらう。** とくに**カメラ経路**。
   E2E は「写真なしで進む」側しか通せない（実ブラウザでカメラを扱えない）ので、
   **そこだけ機械で見られていない。**
2. **S1〜S6 の実機確認**（持ち越し。S2・S3 が未確認）。デモの3タップで作られる案件を
   そのまま使えば、設定を入れずに書類画面まで行ける。
   - S2: 行ブロックを親指で押せる。押し間違えない ← **未確認**
   - S3: シートが出たら本文16px・タップ48pxで、拡大せずに数量を打てる ← **未確認**

   **S1〜S5 のどれかが崩れていたら、見せ方の作り直しになる。** 実装を進めずに報告して決める。
3. `/projects/spike-document` が固定のサンプルデータのままで、案件と繋がっていない。
   **S1〜S5 の結果次第で作り直しになる箇所**なので、確認をもらってから着手する。
4. **短いURLを使えるようにするか**（利用者の作業）。`renovation-estimate-app.vercel.app`
   （`-web` 無し）は 404。`-web` は monorepo の都合がURLに漏れているだけで、
   商談で口頭で伝えると必ず間違える。Vercel の Project → Settings → Domains で追加できる。
5. デモの入口に前段（Vercel の WAF 等）で流量制限を掛けるかの判断（未決。
   判断の材料は `docs/design.md` 6-1）。
6. 期限切れのデモを確実に消すための定時実行を入れるかの判断（未決。同上）。
7. 社印の取り扱い（会社設定に画像を持たせるか、印影なしで運用するか）。

## ④まだ埋まっていない前提

- 依頼提示後に写真を追加したときの扱い（法的評価は未確認）
- 期限を過ぎた回答の扱い
- 諸経費率の実際の数字、見積有効期限の日数（デモ用に決め打ちでよい）
- **明細ごとに別の掛率を持たせるか**（採用の掛率そのものは 1 に決めた。`docs/design.md` 7章）
- 本番で実在の顧客情報を扱うときの社外ホスティング可否
- **法定③（設計図書＝写真＋数量）の充足判定条件**（未決）。
  **送信ゲートは③を検査していない**（`lib/db/submissionGate.ts` が見るのは
  法定④⑤⑥⑧の9スロットと⑦の12区分だけ）。写真が0枚・数量が全部0でも送信できる。
  実運用に出す前に、充足条件を一次情報から決めて判定とテストを足すこと。
  デモの範囲では、写真と数量を入れて見せるので露見しない。

## ⑤分かっているが直していないこと

- `seedDemoData` は複数の insert を分けて投げるので、途中で失敗すると中途半端な行が残る
  （CodeRabbit の指摘）。1つのトランザクション（DB関数）に寄せると直る。
  デモは作り直しが効くので範囲外にした。
- **`lib/db/` の一覧系が、1000件を超えた分を黙って落とす**（PostgREST の既定の上限。
  アプリ側で範囲を指定していない）。利用者からは「登録したのに出てこない」に見える。
  下請1000社は非現実的だが、案件は年数で超えうる。
- `docs/failures.md` の append-only を機械で止めていない（2回破りかけている）。
- **squash マージ後にブランチを作り直す規則を、機械で止めていない**（4回違反している）。
- `auto-merge.yml` がイベントを取りこぼすと、全緑のPRが誰にも気づかれず止まる
  （塞ぎ方の候補は `docs/failures.md` 2026-08-06 に書いた）。
